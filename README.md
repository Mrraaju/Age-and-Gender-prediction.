# 🎯 Age and Gender Prediction using Machine Learning

## 📘 Project Overview
The **Age and Gender Prediction System** is a machine learning-based project that predicts the **age group** and **gender** of a person from an input facial image.

The system uses **OpenCV** for face detection, **TensorFlow/Keras** for model training, and **Node.js** for backend integration.

The user interacts with a clean, responsive interface built using **HTML**, **JavaScript**, and **Tailwind CSS**.
